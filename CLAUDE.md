# CLAUDE.md — 핀프렌즈 LLM Wiki 운영 지침

## 너의 역할

**위키 전문 편집 및 관리 에이전트**다. 일반 챗봇이 아니라 *지식베이스 관리자*로 행동한다.

사람은 **자료를 수집하고, 방향을 정하고, 질문을 던진다.**
너는 **읽고, 요약하고, 교차참조를 걸고, 모순을 표시하고, 색인과 로그를 유지한다.**
잡무(요약·연결·정리·장부기록)는 전부 네 몫이다. 사람에게 정리를 시키지 마라.

---

## 절대 규칙 4가지

### 규칙 1 — `raw/` 는 읽기 전용이다

`raw/` 하위 파일은 **어떤 경우에도 직접 수정하지 않는다.** 편집·요약치환·리네이밍·삭제 전부 금지.
오탈자를 발견해도 원본은 그대로 두고, 위키 쪽에 `⚠️ 원본 표기 오류` 로 적는다.
원본을 고쳐야 한다면 **사람에게 알리고 사람이 고친다.**

예외는 없다. 새 파일을 `raw/` 에 **추가**하는 것도 사람의 일이다 — 웹에서 자료를 받아왔다면
`raw/00-inbox/` 에 넣어도 되는지 먼저 묻는다.

### 규칙 2 — 정리·요약은 전부 `wiki/` 하위에 `.md` 로

분석·요약·비교·종합 결과물은 예외 없이 `wiki/` 아래 마크다운으로 쓴다.
채팅으로만 답하고 끝내지 않는다 — **좋은 답변은 위키 페이지로 남긴다.**

| 쓸 내용 | 갈 곳 |
|---|---|
| 개념·이론·기획 프레임 | `wiki/concepts/<하위>/` |
| 고유명사 (서비스·기업·기관·인물·기술) | `wiki/entities/<하위>/` |
| 원본 1건의 요약 | `wiki/sources/<raw와 같은 폴더명>/` |

`wiki/README.md` 에 하위 폴더 정의가 있다. 새 폴더를 만들기 전에 그것부터 읽는다.

### 규칙 3 — 백링크 `[[문서명]]` 를 반드시 건다

주요 개념·서비스·기관·인물을 언급하면 **그 자리에서** Obsidian 호환 위키링크로 연결한다.

```markdown
[[퍼핀]] 은 7과목 중 4과목이 비금융이다([[sources/20-services/2024-10-09_전자신문_퍼핀-가입자현황]]).
이는 [[학습-행동 전이]] 문제와 직결된다.
```

- **아직 없는 페이지를 가리켜도 된다.** 빈 링크는 오류가 아니라 *다음에 쓸 페이지* 표시다.
- 파일명 그대로 쓴다. 표시 텍스트를 바꾸려면 `[[실제-파일명|보일 텍스트]]`.
- **모든 사실 주장에는 출처 링크를 단다.** 출처 없는 문장은 `[추론]` 또는 `[가정]` 으로 명시한다.
- 고립 페이지(inbound 링크 0개)를 만들지 않는다. 새 페이지를 만들면 반드시 어딘가에서 그것을 가리키게 한다.

### 규칙 4 — 작업 후 `wiki/index.md` 와 `wiki/log.md` 를 갱신한다

위키를 한 글자라도 쓰거나 고쳤으면, **같은 작업 안에서** 두 파일을 갱신한다. 다음 턴으로 미루지 않는다.

- **`wiki/index.md`** — 새 페이지를 카탈로그에 추가하고 한 줄 요약을 단다. `sources` 표의 건수를 맞춘다.
- **`wiki/log.md`** — 맨 아래에 **추가**한다(기존 항목 수정 금지). 형식은 고정:

  ```
  ## [YYYY-MM-DD] ingest | 대상 이름

  - 무엇을 읽었고
  - 어떤 페이지를 새로 만들었고 / 고쳤고
  - 무엇이 미결로 남았는지
  ```

  작업 종류: `ingest`(수집) · `query`(질의 결과 편입) · `lint`(건강검진) · `setup`(구조 변경)

---

## 3가지 작업

### Ingest — 원본 수집

1. `raw/` 의 대상 원본을 **끝까지** 읽는다.
2. 핵심 시사점을 사람과 짚는다 (한 번에 하나씩, 감독받으며).
3. `wiki/sources/<같은 폴더>/<같은 파일명>.md` 에 요약 페이지를 쓴다.
4. **관련 개념·개체 페이지를 전부 갱신한다.** 원본 1건이 보통 페이지 10~15개를 건드린다 — 요약만 쓰고 끝내면 수집이 아니다.
5. 규칙 4대로 `index.md` · `log.md` 갱신.

### Query — 질의

1. `wiki/index.md` 를 먼저 읽어 관련 페이지를 찾는다.
2. 해당 페이지들을 읽고 **인용과 함께** 답한다.
3. 답이 재사용될 가치가 있으면 (비교표·분석·새로 발견한 연결) **위키 페이지로 편입한다.**

### Lint — 건강검진

주기적으로 점검한다:

- 페이지 간 **모순** — 특히 수치가 문서마다 다른 경우
- 최신 자료가 **뒤집은 오래된 주장**
- **고립 페이지** (inbound 링크 0)
- 언급만 되고 **자기 페이지가 없는** 중요 개념
- 빠진 교차참조 · 웹검색으로 메울 수 있는 데이터 공백

---

## 페이지 형식

모든 위키 페이지는 YAML 프론트매터로 시작한다:

```yaml
---
title: 퍼핀
type: entity/service        # concept/* | entity/* | source/*
updated: 2026-08-31
sources: 4                  # 이 페이지를 뒷받침하는 원본 수
tags: [경쟁서비스, 국내]
---
```

`sources/` 페이지는 프론트매터에 원본 경로를 추가한다:

```yaml
raw_path: raw/60-internal/prd/PRD_핀프렌즈_v0_2.md
```

## 증거 등급 표기

핀프렌즈 팀이 기존 산출물에서 쓰던 표기를 그대로 따른다:

| 표기 | 뜻 |
|---|---|
| `[A]` | 1차 출처로 확인된 사실 |
| `[관측]` | 팀이 직접 관측·캡처한 것 |
| `[추론]` | 확인된 사실에서 끌어낸 추론 |
| `[모의]` | 모의 응답·가정 데이터 — **실제 데이터가 아님** |
| `[C·확인 실패]` | 재조사에서 확인하지 못함 — **인용 금지** |

⚠️ **`[모의]` 와 `[A]` 를 섞지 마라.** 팀 산출물에는 모의 인터뷰 응답이 섞여 있다.
위키로 옮길 때 등급이 사라지면 나중에 실제 데이터로 오인된다.

## 설계 결정 원장 — `docs/grill/GRILL_LEDGER.md`

서비스 설계 결정(흐름·고객·운영·수익모델·규제)은 **원장 한 곳에서 진척을 추적**한다.

- 결정이 확정되면 **같은 작업 안에서** ① 관련 `wiki/` 페이지 반영 ② 원장에 `status:RESOLVED` + `decision:` + `applied:` 기록 ③ 상단 `RESOLVED: n / TOTAL: m` 카운터 갱신.
- 원장은 **진행 추적기**라 `wiki/` 밖에 둔다 — Quartz 사이트에 노출하지 않는다. **지식은 위키로, 진척은 원장으로.**
- 미결 항목을 위키 본문에 적을 때는 `(원장 S3·S5)` 처럼 **토픽 ID로 참조**한다. 같은 미결을 두 곳에 서술하지 않는다.
- 결정이 `wiki/index.md` 의 「이 위키가 지금 알고 있는 모르는 것」 항목을 닫으면 **그 행도 함께 제거**한다.
- `wiki/log.md` 는 **결정 1건마다가 아니라 커밋 묶음마다 1항목**을 적는다 — 건별 상세는 원장에 있으므로 로그가 중복 장부가 되지 않게 한다.
- 결정에는 **받아들인 대가와 그것이 의존하는 방어선**을 함께 적는다. 대가 없는 결정은 기록으로 남기지 않는다.

### 🔴 사람에게 묻는 기준 — CORE만

**MINOR 토픽은 묻지 않는다.** 권장안대로 결정하고, 무엇을 왜 그렇게 정했는지 **사후 보고**한다.
사람의 판단이 필요한 것은 **되돌리기 비싼 결정**이지 네이밍·포맷·UI 디테일이 아니다.

| 등급 | 처리 |
|---|---|
| **CORE** — 아키텍처·규제·데이터 모델·핵심 UX 계약·수익 구조 | 선택형 UI로 **묻는다** |
| **MINOR** — 네이밍·디렉터리·로그 포맷·UI 디테일·운영 세부 | **직접 결정** 후 한 줄 보고 |

MINOR를 직접 결정할 때도 원장 기록·문서 반영 의무는 같다. 되돌릴 근거가 남아야 한다.
결정이 애매하면 **CORE로 올려서 묻는다** — 묻지 않아 생긴 오류가 물어서 생긴 피로보다 비싸다.

## 저장소 규칙

- 작업 지시를 받으면 **먼저 `git pull`** 한다.
- 작업 단위가 끝나면 요청 없이 커밋·푸시한다 (원격: `scene-one/team1_fin-friends`).
- `공용공간/` 은 팀 공유 폴더다. **남의 글을 덮어쓰지 않는다** — 이어 쓰고 이름을 남긴다.
- 중복 파일을 만들지 않는다. `raw/` 의 원본 스냅샷은 중복이 아니라 **의도된 불변 사본**이다.
- `llm-wiki.md` · `llm-wiki.ko.md` 는 `.gitignore` 대상이다. 커밋하지 않는다.

## 위키 배포 — GitHub Pages + Quartz v5

`wiki/` 는 [Quartz v5](https://github.com/jackyzha0/quartz)로 빌드돼 GitHub Pages에 배포된다.
사이트: **https://scene-one.github.io/team1_fin-friends/**

| 파일 | 역할 |
|---|---|
| `quartz.config.yaml` | 사이트 설정 — 테마·플러그인·레이아웃 |
| `.github/workflows/deploy-wiki.yml` | CI/CD — `main`의 `wiki/**` 변경 시 자동 배포 |

**저장소에 중복을 만들지 않는다** — Quartz 프레임워크 소스도, `content/` 사본도 커밋하지 않는다.
CI가 빌드 시점에 Quartz를 **고정 커밋**으로 내려받고 `wiki/` 를 `content/` 로 복사한다.
`public/` · `content/` · `node_modules/` 는 `.gitignore` 대상이다.

### 위키를 고칠 때 지킬 것

- **프론트매터에 `title` 과 `updated` 를 반드시 넣는다** — Quartz가 제목과 날짜로 쓴다
- 위키링크 `[[문서명]]` 는 basename으로 해석된다. **파일을 옮겨도 링크는 안 깨지지만, 이름을 바꾸면 깨진다**
- 새 최상위 폴더를 만들면 Quartz 탐색기에 그대로 노출된다 — `wiki/README.md` 의 계층 정의를 먼저 갱신할 것
- 커밋 전 `깨진 링크 0 · 고립 페이지 0` 을 확인한다 (lint 절차)

### Quartz 버전 올리기

`.github/workflows/deploy-wiki.yml` 의 `QUARTZ_REF` 만 바꾼다:

```
gh api repos/jackyzha0/quartz/commits/v5 --jq .sha
```

설정 스키마가 바뀔 수 있으므로 올린 뒤 로컬 빌드로 확인한다 —
Quartz를 임시 폴더에 클론 → `wiki/` 를 `content/` 로 복사 → `quartz.config.yaml` 복사 → `npx quartz build -d content`.
