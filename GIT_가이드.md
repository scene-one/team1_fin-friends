# Git 기본 가이드 (비전공자용)

> 이 문서만 알면 이번 프로젝트는 충분합니다. 그 이상은 몰라도 됩니다.

---

## 1. Git이 뭔가 — 30초 설명

**구글 드라이브 공유폴더**라고 생각하세요. 딱 하나 차이가 있습니다.

| | 구글 드라이브 | Git (GitHub) |
|---|---|---|
| 동기화 | **자동** | **수동** — 내가 "올려" "받아" 해줘야 함 |
| 기록 | 최근 것만 | **모든 변경이 다 남음** (언제든 되돌리기 가능) |
| 동시 편집 | 실시간 합쳐짐 | 같은 파일 건드리면 **충돌** |

그래서 명령어가 필요한 겁니다. 자동이 아니니까요.

### 용어 4개만

| 용어 | 뜻 | 쉽게 |
|---|---|---|
| **repo** (리포) | 프로젝트 폴더 | 공유폴더 |
| **commit** (커밋) | 변경 사항 저장 | **내 컴퓨터에만** 저장. 아직 팀은 못 봄 |
| **push** (푸시) | 서버에 올리기 | **업로드** |
| **pull** (풀) | 서버에서 받기 | **다운로드** |

⚠️ 헷갈리는 것 1위: **커밋만 하면 아무도 못 봅니다.** 커밋 → 푸시까지 해야 올라갑니다.

---

## 2. 비전공자는 명령어 쓰지 마세요 — GitHub Desktop 추천

터미널 명령어 배우는 데 시간 쓰지 마세요. **GitHub Desktop**(무료 앱) 쓰면 버튼 클릭으로 끝납니다.

**설치:** https://desktop.github.com

### 처음 한 번만 (5분)

1. GitHub Desktop 설치 후 실행
2. GitHub 계정으로 로그인
3. `File` → `Clone repository` → `URL` 탭
4. 여기 붙여넣기 → `scene-one/team1_fin-friends`
5. 저장할 위치 고르고 `Clone`

끝. 이제 내 컴퓨터에 폴더가 생겼습니다.

### 매일 하는 것 (30초)

| 순서 | 언제 | 뭘 누르나 |
|---|---|---|
| **1** | **작업 시작할 때** | 왼쪽 위 `Fetch origin` → `Pull origin` 뜨면 클릭 |
| 2 | | 평소처럼 파일 편집 (메모장이든 뭐든) |
| **3** | **작업 끝나면** | 왼쪽 아래에 메시지 쓰고 `Commit to main` |
| **4** | | 오른쪽 위 `Push origin` |

**1번을 안 하면 충돌 납니다.** 이거 하나만 습관 들이세요.

---

## 3. 터미널로 하고 싶다면 (4줄)

```bash
cd ~/Desktop/team1_fin-friends    # 폴더로 이동

git pull                          # ① 시작 전: 남들 작업 받기

# ... 파일 편집 ...

git add -A                        # ② 올릴 것 다 담기
git commit -m "혜원 페르소나 초안"   # ③ 저장
git push                          # ④ 올리기
```

**② ③ ④는 항상 세트**입니다. 하나 빠지면 안 올라갑니다.

지금 상태가 궁금할 때:
```bash
git status      # 뭐가 바뀌었나
git log --oneline -5    # 최근 5개 기록
```

---

## 4. 우리 팀 규칙 3개 🔴

이 3개만 지키면 사고 안 납니다.

### 규칙 1. 시작할 때 무조건 pull

작업 시작 전에 **항상** 받아오세요. 이거 안 하고 작업하면 나중에 충돌 납니다.
아침에 앱 켜듯이 습관으로.

### 규칙 2. 자기 폴더 안에서만 작업

```
유림/  ← 유림만
병윤/  ← 병윤만
혜원/  ← 혜원만
하영/  ← 하영만
```

**폴더를 나눠놓은 이유가 이겁니다.** 서로 다른 파일을 건드리면 충돌이 아예 안 납니다.

### 규칙 3. 공용 파일은 미리 말하고

`README.md` `WBS.md` 같은 루트 파일은 여러 명이 동시에 고치면 충돌합니다.
→ **단톡에 "나 WBS 좀 고칠게" 한마디 하고** 고치세요.

---

## 5. ⚠️ 중요: PPT랑 엑셀은 Git에 올리지 마세요

이번 프로젝트 최종 산출물이 **PPT와 엑셀**인데, 이건 Git이 제일 못하는 것입니다.

**왜냐면:**
- Git은 텍스트 파일만 합칠 수 있습니다
- PPT/엑셀은 컴퓨터가 못 읽는 형식이라 **충돌 나면 해결이 불가능**합니다
- 둘 중 하나를 통째로 버려야 합니다 → **작업 날아감**

**그래서 이렇게 하세요:**

| 파일 종류 | 어디에 |
|---|---|
| 발표자료 (PPT) | **구글 슬라이드** — 실시간 동시 편집 |
| 근거 엑셀 | **구글 스프레드시트** — 실시간 동시 편집 |
| 결정 기록 | **구글 스프레드시트** 또는 노션 |
| 메모 · 초안 · 정리 (.md, .txt) | **Git** ⭕ |
| 캡처 이미지 | Git ⭕ (단, 용량 작게) |

→ Git에는 **구글 문서 링크만** 적어두세요. README에 링크 모아두면 됩니다.

**한 줄 요약:** 진짜 산출물은 구글 문서로, Git은 메모·계획·기록용으로.

---

## 6. 사고 났을 때

### "충돌(conflict)이 났대요"

당황하지 마세요. **아무것도 날아가지 않았습니다.**

1. **일단 단톡에 스크린샷** 올리기
2. 급하면: 내 작업 파일을 **바탕화면에 복사해두고** → 폴더 지우고 → 다시 clone → 복사해둔 파일 붙여넣기
   - 무식하지만 확실하고, 4일짜리 프로젝트에선 이게 제일 빠릅니다

### "실수로 파일을 지웠어요"

커밋한 적 있으면 **100% 복구됩니다.** 단톡에 말하세요.

### "push가 안 돼요 / rejected 떴어요"

거의 항상 **pull을 안 해서** 그렇습니다.
```bash
git pull
git push
```
이 순서로 다시 해보세요.

---

## 7. 절대 하지 말 것 🚫

| 명령어 | 왜 위험한가 |
|---|---|
| `git push -f` / `--force` | **남의 작업을 통째로 날립니다.** 절대 금지 |
| `git reset --hard` | **내 작업이 복구 불가능하게 사라집니다** |
| 100MB 넘는 파일 올리기 | 아예 안 올라가거나 리포가 느려집니다 |

**인터넷에서 본 명령어를 이해 못 한 채로 복붙하지 마세요.** 특히 `-f` `--force` `--hard` 가 들어간 것.
모르겠으면 단톡에 물어보는 게 훨씬 빠릅니다.

---

## 8. 커밋 메시지 쓰는 법

거창하게 안 써도 됩니다. **"누가 뭘 했는지"** 만 알면 됩니다.

✅ 좋은 예
```
혜원 페르소나 초안
병윤 시장 크기 계산 추가
WBS 8/20 일정 수정
```

❌ 나쁜 예
```
수정
ㅁㄴㅇㄹ
asdf
update
```

나중에 "그거 언제 바꿨더라?" 찾을 때 본인이 고생합니다.

---

## 9. 한 장 요약 (이것만 외우세요)

```
작업 시작 →  pull  (받기)
   ↓
  작업
   ↓
작업 끝  →  commit  (저장)  →  push  (올리기)
```

**규칙 3개**
1. 시작할 때 무조건 pull
2. 자기 폴더에서만 작업
3. 공용 파일 고칠 땐 단톡에 한마디

**PPT·엑셀은 구글 문서로. Git은 메모·계획용.**

막히면 혼자 30분 붙잡지 말고 단톡에 스크린샷 올리세요.
